export const pieces: string[] = ['I', 'J', 'L', 'S', 'Z', 'O', 'T'];

export const default_config: { [opt: string]: any } = {
    "square-length": 30,
    "fps": 60
}

// TODO: Buscar las tablas de kicks y codificarlas
export const kicks: any = 0;